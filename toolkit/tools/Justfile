help:
	@just --list

version:
	@just --version
	@restic version
	@kopia --version
	@bw --version
	@ansible --version
	@medusa version

backup-vaultwarden:
	ANSIBLE_STDOUT_CALLBACK=debug ansible-playbook -vvv /ansible/vaultwarden/backup-vaultwarden-export.yaml

backup-vault:
	ansible-playbook /ansible/vault/backup-vault-export.yaml