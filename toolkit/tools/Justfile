help:
	@just --list

version:
	@just --version
	@restic version
	@kopia --version
	@bw --version
	@ansible --version
	@medusa version

# Backup application
backup tool app type:
    ansible-playbook /ansible/backup-{{tool}}-{{app}}-{{type}}.yaml

backup-vaultwarden:
	ansible-playbook /ansible/backup-vaultwarden-export.yaml

backup-vault:
	ansible-playbook /ansible/backup-vault-export-restic.yaml