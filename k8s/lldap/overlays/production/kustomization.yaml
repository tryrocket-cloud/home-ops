apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - pvc.yaml
  - ldap-external-secret.yaml
  - smtp-external-secret.yaml
  - ingressRouteTCP.yaml

namespace: lldap

images:
  - name: lldap/lldap:latest
    newTag: stable
