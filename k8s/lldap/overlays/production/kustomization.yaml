apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - pvc.yaml
  - ldap-config-external-secret.yaml
  - smtp-config-external-secret.yaml

namespace: lldap

images:
  - name: lldap/lldap:latest
    newTag: stable
